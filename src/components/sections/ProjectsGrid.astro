---
import ProjectCard from '../ui/ProjectCard.astro';

interface Props {
  projects: any[];
  t?: any;
}

const { projects, t } = Astro.props;

// Extract translation safely
const translations = t ?? {};
const baseTitle = t?.title ?? 'Trabajos Recientes';
const words = baseTitle.split(' ');
const lastWord = words.pop();
const isHighlighted = lastWord?.toLowerCase().includes('recientes');
const highlightClass = isHighlighted ? 'highlight' : '';
const displayWords = words;
---

<section id="projects" class="projects-section container">
  <div class="section-header">
    <h2 class="section-title">
      {displayWords.length > 0 && (
        <span>
          {displayWords.join(' ')}
          {highlightClass ? (
            <span class={highlightClass}>
              {lastWord}
            </span>  
          ) : lastWord}
        </span>
      )}
      
      {!displayWords.length && (
        <>
          {lastWord ? (
            <span class={highlightClass}>
              {lastWord}
            </span>
          ) : {baseTitle}}
        </>
      )}
    </h2>
    <p class="section-subtitle">Una selección de proyectos escalables construidos con tecnologías modernas.</p>
  </div>

  <div class="projects-grid">
    {projects.map(project => (
      <ProjectCard project={project} t={translations} />
    ))}
  </div>
</section>

<style>
  .projects-section {
    padding: 6rem 1.5rem;
  }

  .section-header {
    margin-bottom: 4rem;
    text-align: center;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  
  .highlight {
    color: var(--accent-blue);
  }

  .section-subtitle {
    color: var(--text-secondary);
    font-size: 1.1rem;
    max-width: 600px;
    margin: 0 auto;
  }

  /* Implementación de CSS GRID pura */
  .projects-grid {
    display: grid;
    /* auto-fit llena el espacio con columnas que miden al menos 320px */
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
  }
</style>
